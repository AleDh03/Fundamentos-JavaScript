// Las funciones son fracciones de código que se reutilizan. Se definen con function nameFunction() {}.
// JavaScript es un lenguaje interpretado. por lo que intentara ejecutar el código sin importar si los parámetros estén invertidos o incluso incompletos.

var name = "Alejandro",
  age = 25;

// hay una función llamada 'printAge()' {que toma un string}"
function printAge(n, e) {
  console.log(`${n} tiene ${e} años`);
}
printAge(name, age); // Declaración de la funciónHay que llamar la función, para que se ejecute

// printAge("Karijen", 36);
// printAge("Lía", 3);
// printAge("Roger", 30);
// printAge(12, "some");
